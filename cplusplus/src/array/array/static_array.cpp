#include "static_array.h"

template <typename T>
StaticArray<T>::StaticArray<T>(int capacity)
{

};

template <typename T>
void StaticArray<T>::insert(T value, int location)
{

};

template <typename T>
void StaticArray<T>::remove(T value, int location)
{

};